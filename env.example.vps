# === NEXTCLOUD WRAPPER VPS CONFIGURATION ===

# Nextcloud server settings (MODIFICA QUESTI!)
NC_BASE_URL=https://your-nextcloud.domain.com
NC_ADMIN_USER=admin
NC_ADMIN_PASS=your_admin_password_here

# Default settings ottimizzati per VPS
NC_DEFAULT_FS_PERCENTAGE=0.02
NC_DEFAULT_QUOTA=100G
NC_MOUNT_BASE_DIR=/mnt/nextcloud

# Service settings
NC_SERVICE_USER=root
NC_AUTO_ENABLE_SERVICES=true

# Performance settings ottimizzati
RCLONE_MOUNT_OPTIONS="--vfs-cache-mode writes --vfs-cache-max-size 2G --buffer-size 64M --dir-cache-time 10m"

# Logging
NC_LOG_LEVEL=INFO
NC_LOG_FILE=/var/log/nextcloud-wrapper.log

# Security
NC_REQUIRE_SSL=true
NC_VERIFY_SSL=true
NC_API_TIMEOUT=30

# Quota settings per VPS
NC_HOME_BASE_PATH=/home
NC_FILESYSTEM_TYPE=btrfs

# === ISTRUZIONI ===
# 1. Modifica NC_BASE_URL con il tuo server Nextcloud
# 2. Imposta NC_ADMIN_USER e NC_ADMIN_PASS con credenziali admin valide
# 3. Ricarica ambiente: source .env
# 4. Test: nextcloud-wrapper config
# 5. Setup primo utente: nextcloud-wrapper setup test.com password123

# === ESEMPI CONFIGURAZIONE ===
# 
# Hosting provider piccolo:
# NC_DEFAULT_QUOTA=50G
# NC_DEFAULT_FS_PERCENTAGE=0.02  # 1GB filesystem per 50GB Nextcloud
#
# Server aziendale:
# NC_DEFAULT_QUOTA=500G
# NC_DEFAULT_FS_PERCENTAGE=0.01  # 5GB filesystem per 500GB Nextcloud
#
# Server development:
# NC_DEFAULT_QUOTA=20G
# NC_DEFAULT_FS_PERCENTAGE=0.1   # 2GB filesystem per test
